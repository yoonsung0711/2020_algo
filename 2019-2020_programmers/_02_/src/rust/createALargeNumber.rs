fn main() {

}

fn solution(number: &str, mut k: i32) -> String {
    let answer: String = String::new();
    let resultLen = number.len() - k as usize;
    let mut pos: i32 = 0;
    while k != 0 {
        let maxValue = -1;
        let maxPos = 0;
        while i <= pos + k {
        }
        k = k - (maxPos - pos);
        pos = maxPos + 1;

        if (resultLen == answer.len()) {
            return answer;
        }
        if (k == 0) {
        }
    }
    return "".to_string();
}
