use std::collections::VecDeque;

fn main() {

}

fn solution(cacheSize: i32, cities: Vector<&str>) -> i32 {
    let answer: i32 = 0;
    let dq : VecDeque<&str>;
    if cacheSize == 0 {
        return cities.len() * 5;
    } else {
        
    }
    return 0;
}

